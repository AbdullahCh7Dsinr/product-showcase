<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Showcase</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcode-generator/1.4.4/qrcode.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            color: white;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .add-product-section {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 40px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #e1e5e9;
            border-radius: 10px;
            font-size: 16px;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
        }

        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }

        .btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
        }

        .product-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 25px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .product-name {
            font-size: 1.4rem;
            font-weight: 700;
            color: #333;
        }

        .delete-btn {
            background: #ff4757;
            color: white;
            border: none;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            cursor: pointer;
            font-size: 14px;
            transition: background 0.3s ease;
        }

        .delete-btn:hover {
            background: #ff3838;
        }

        .product-description {
            color: #666;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .product-video {
            width: 100%;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .product-image {
            width: 100%;
            max-height: 250px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .media-tabs {
            display: flex;
            margin-bottom: 20px;
            background: #f8f9fa;
            border-radius: 10px;
            padding: 5px;
        }

        .media-tab {
            flex: 1;
            padding: 10px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            background: transparent;
            border: none;
            color: #666;
        }

        .media-tab.active {
            background: white;
            color: #667eea;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .media-content {
            display: none;
        }

        .media-content.active {
            display: block;
        }

        .file-input-container {
            position: relative;
            display: inline-block;
            cursor: pointer;
            width: 100%;
        }

        .file-input-container input[type=file] {
            position: absolute;
            opacity: 0;
            width: 100%;
            height: 100%;
            cursor: pointer;
        }

        .file-input-label {
            display: block;
            padding: 12px;
            border: 2px dashed #e1e5e9;
            border-radius: 10px;
            text-align: center;
            transition: all 0.3s ease;
            background: #f8f9fa;
        }

        .file-input-label:hover {
            border-color: #667eea;
            background: #f0f2ff;
        }

        .image-preview {
            margin-top: 15px;
            text-align: center;
        }

        .preview-img {
            max-width: 100%;
            max-height: 200px;
            border-radius: 10px;
            border: 2px solid #e1e5e9;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .qr-section {
            text-align: center;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
        }

        .qr-code {
            margin: 15px 0;
        }

        .url-display {
            background: #e9ecef;
            padding: 10px;
            border-radius: 5px;
            margin: 10px 0;
            font-family: monospace;
            word-break: break-all;
            font-size: 12px;
        }

        .no-products {
            text-align: center;
            color: rgba(255, 255, 255, 0.8);
            font-size: 1.2rem;
            margin: 50px 0;
        }

        .hidden {
            display: none !important;
        }

        @media (max-width: 768px) {
            .products-grid {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🛍️ Product Showcase</h1>
            <p>Professional product display with QR codes</p>
        </div>

        <!-- Admin Login -->
        <div id="loginSection" class="add-product-section">
            <h3>Admin Login</h3>
            <div class="form-group">
                <input type="password" id="adminPassword" placeholder="Enter admin password (default: admin123)">
                <button type="button" onclick="loginAdmin()" class="btn" style="margin-top: 10px;">Login as Admin</button>
            </div>
        </div>

        <!-- Add Product Form (Hidden until admin login) -->
        <div id="adminSection" class="add-product-section hidden">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
                <h2>Add New Product</h2>
                <div>
                    <button type="button" onclick="exportData()" class="btn" style="margin-right: 10px; background: #28a745;">💾 Export Data</button>
                    <button type="button" onclick="importData()" class="btn" style="background: #17a2b8;">📁 Import Data</button>
                    <input type="file" id="importFile" accept=".json" style="display: none;" onchange="handleImportFile(event)">
                </div>
            </div>
            <div class="form-group">
                <label for="productName">Product Name</label>
                <input type="text" id="productName" required>
            </div>
            
            <div class="form-group">
                <label for="productDescription">Description</label>
                <textarea id="productDescription" placeholder="Describe your product..."></textarea>
            </div>
            
            <div class="form-group">
                <label for="productImage">Product Photo</label>
                <div class="file-input-container">
                    <input type="file" id="productImage" accept="image/*" onchange="previewImage()">
                    <label for="productImage" class="file-input-label">
                        📷 Click to upload or drag & drop an image
                    </label>
                </div>
                <div id="imagePreview" class="image-preview hidden">
                    <img id="previewImg" class="preview-img">
                </div>
            </div>
            
            <div class="form-group">
                <label for="productVideo">Video URL (YouTube, Vimeo, or direct video link)</label>
                <input type="url" id="productVideo" placeholder="https://www.youtube.com/watch?v=...">
            </div>
            
            <button type="button" onclick="addNewProduct()" class="btn">Add Product</button>
        </div>

        <div id="productsContainer">
            <div class="no-products">
                No products added yet. Admin can add products above!
            </div>
        </div>

        <!-- Website QR Code -->
        <div class="add-product-section">
            <h2>Share Your Website</h2>
            <div class="qr-section">
                <div style="margin-bottom: 20px;">
                    <label for="websiteUrl" style="display: block; margin-bottom: 10px; font-weight: 600;">Enter your website URL:</label>
                    <input type="url" id="websiteUrl" placeholder="https://your-domain.com or https://your-hosting-url.com" 
                           style="width: 100%; padding: 10px; border: 2px solid #e1e5e9; border-radius: 5px; margin-bottom: 10px;">
                    <button onclick="updateWebsiteQR()" class="btn">Generate QR Code</button>
                </div>
                <div id="websiteQRContainer" style="display: none;">
                    <p>Scan this QR code to access your website:</p>
                    <div id="websiteQR" class="qr-code"></div>
                    <div class="url-display" id="websiteURL"></div>
                </div>
                <div style="background: #fff3cd; border: 1px solid #ffeaa7; border-radius: 5px; padding: 15px; margin-top: 15px;">
                    <h4>📝 How to make QR codes work:</h4>
                    <p><strong>Option 1 - Host Online:</strong></p>
                    <ul style="margin: 10px 0; padding-left: 20px;">
                        <li>Upload this HTML file to a web hosting service</li>
                        <li>Free options: GitHub Pages, Netlify, Vercel</li>
                        <li>Enter the hosted URL above to generate working QR codes</li>
                    </ul>
                    <p><strong>Option 2 - Local Network:</strong></p>
                    <ul style="margin: 10px 0; padding-left: 20px;">
                        <li>Use a local server (like XAMPP, Live Server extension)</li>
                        <li>Find your local IP address (like http://192.168.1.100:3000)</li>
                        <li>Enter that URL above - works for devices on same WiFi</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <script>
        let products = [];
        let isAdmin = false;
        const ADMIN_PASSWORD = "admin123"; // Change this to your desired password
        const STORAGE_KEY = "productShowcaseData";

        // Save products to localStorage
        function saveProducts() {
            try {
                localStorage.setItem(STORAGE_KEY, JSON.stringify(products));
            } catch (error) {
                console.log('Could not save to localStorage:', error);
            }
        }

        // Load products from localStorage
        function loadProducts() {
            try {
                const saved = localStorage.getItem(STORAGE_KEY);
                if (saved) {
                    products = JSON.parse(saved);
                    return true;
                }
            } catch (error) {
                console.log('Could not load from localStorage:', error);
            }
            return false;
        }

        // Admin login function
        function loginAdmin() {
            const password = document.getElementById('adminPassword').value;
            if (password === ADMIN_PASSWORD) {
                isAdmin = true;
                document.getElementById('loginSection').classList.add('hidden');
                document.getElementById('adminSection').classList.remove('hidden');
                renderProducts();
                alert('✅ Welcome Admin! You can now add and edit products.');
                document.getElementById('adminPassword').value = '';
            } else {
                alert('❌ Incorrect password. Try again.');
            }
        }

        // Image preview function
        function previewImage() {
            const file = document.getElementById('productImage').files[0];
            const preview = document.getElementById('imagePreview');
            const previewImg = document.getElementById('previewImg');
            
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    previewImg.src = e.target.result;
                    preview.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            } else {
                preview.classList.add('hidden');
            }
        }

        // Add new product function
        function addNewProduct() {
            const name = document.getElementById('productName').value.trim();
            const description = document.getElementById('productDescription').value.trim();
            const video = document.getElementById('productVideo').value.trim();
            const imageFile = document.getElementById('productImage').files[0];
            
            if (!name) {
                alert('❌ Please enter a product name');
                return;
            }
            
            // Handle image upload
            if (imageFile) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const product = {
                        id: Date.now(),
                        name: name,
                        description: description,
                        video: video,
                        image: e.target.result
                    };
                    products.push(product);
                    saveProducts(); // Save after adding
                    renderProducts();
                    clearForm();
                    alert('✅ Product added successfully!');
                };
                reader.readAsDataURL(imageFile);
            } else {
                const product = {
                    id: Date.now(),
                    name: name,
                    description: description,
                    video: video,
                    image: null
                };
                products.push(product);
                saveProducts(); // Save after adding
                renderProducts();
                clearForm();
                alert('✅ Product added successfully!');
            }
        }

        // Clear form function
        function clearForm() {
            document.getElementById('productName').value = '';
            document.getElementById('productDescription').value = '';
            document.getElementById('productVideo').value = '';
            document.getElementById('productImage').value = '';
            document.getElementById('imagePreview').classList.add('hidden');
        }

        // Generate QR code for website access
        function generateWebsiteQR() {
            // Don't generate QR automatically for file:// URLs
            const currentURL = window.location.href;
            if (currentURL.startsWith('file://')) {
                document.getElementById('websiteURL').textContent = 'Please enter your hosted website URL above to generate QR codes';
                return;
            }
            
            document.getElementById('websiteURL').textContent = currentURL;
            const qr = qrcode(0, 'M');
            qr.addData(currentURL);
            qr.make();
            document.getElementById('websiteQR').innerHTML = qr.createImgTag(4);
        }

        // Update website QR with custom URL
        function updateWebsiteQR() {
            const customURL = document.getElementById('websiteUrl').value.trim();
            if (!customURL) {
                alert('❌ Please enter a website URL');
                return;
            }
            
            if (!customURL.startsWith('http://') && !customURL.startsWith('https://')) {
                alert('❌ Please enter a valid URL starting with http:// or https://');
                return;
            }
            
            document.getElementById('websiteURL').textContent = customURL;
            const qr = qrcode(0, 'M');
            qr.addData(customURL);
            qr.make();
            document.getElementById('websiteQR').innerHTML = qr.createImgTag(4);
            document.getElementById('websiteQRContainer').style.display = 'block';
            alert('✅ QR code generated! This will work from any device.');
        }

        // Get video embed HTML
        function getVideoEmbedHTML(url) {
            if (!url) return '';
            
            // YouTube
            const youtubeMatch = url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/)([^&\n?#]+)/);
            if (youtubeMatch) {
                return `<iframe class="product-video" src="https://www.youtube.com/embed/${youtubeMatch[1]}" frameborder="0" allowfullscreen></iframe>`;
            }
            
            // Vimeo
            const vimeoMatch = url.match(/vimeo\.com\/(\d+)/);
            if (vimeoMatch) {
                return `<iframe class="product-video" src="https://player.vimeo.com/video/${vimeoMatch[1]}" frameborder="0" allowfullscreen></iframe>`;
            }
            
            // Direct video
            if (url.match(/\.(mp4|webm|ogg)$/i)) {
                return `<video class="product-video" controls><source src="${url}" type="video/mp4"></video>`;
            }
            
            return `<p>Video: <a href="${url}" target="_blank">${url}</a></p>`;
        }

        // Generate product QR code
        function generateProductQR(productId, productName) {
            const websiteUrl = document.getElementById('websiteUrl').value.trim();
            let productURL;
            
            if (websiteUrl && (websiteUrl.startsWith('http://') || websiteUrl.startsWith('https://'))) {
                productURL = `${websiteUrl}#product-${productId}`;
            } else {
                productURL = `${window.location.href}#product-${productId}`;
            }
            
            const qr = qrcode(0, 'M');
            qr.addData(productURL);
            qr.make();
            return qr.createImgTag(3);
        }

        // Switch media tabs
        function switchTab(productId, mediaType, event) {
            const productCard = document.getElementById(`product-${productId}`);
            const tabs = productCard.querySelectorAll('.media-tab');
            const contents = productCard.querySelectorAll('.media-content');
            
            tabs.forEach(tab => tab.classList.remove('active'));
            contents.forEach(content => content.classList.remove('active'));
            
            event.target.classList.add('active');
            document.getElementById(`${mediaType}-${productId}`).classList.add('active');
        }

        // Render products
        function renderProducts() {
            const container = document.getElementById('productsContainer');
            
            if (products.length === 0) {
                container.innerHTML = '<div class="no-products">No products added yet. Admin can add products above!</div>';
                return;
            }
            
            const html = `
                <div class="products-grid">
                    ${products.map(product => `
                        <div class="product-card" id="product-${product.id}">
                            <div class="product-header">
                                <h3 class="product-name">${product.name}</h3>
                                ${isAdmin ? `<button class="delete-btn" onclick="deleteProduct(${product.id})" title="Delete product">×</button>` : ''}
                            </div>
                            
                            ${product.description ? `<div class="product-description">${product.description}</div>` : ''}
                            
                            ${product.image || product.video ? `
                                <div class="media-tabs">
                                    ${product.image ? `<button class="media-tab active" onclick="switchTab(${product.id}, 'image', event)">📷 Photo</button>` : ''}
                                    ${product.video ? `<button class="media-tab ${!product.image ? 'active' : ''}" onclick="switchTab(${product.id}, 'video', event)">🎥 Video</button>` : ''}
                                </div>
                                ${product.image ? `<div class="media-content active" id="image-${product.id}"><img class="product-image" src="${product.image}" alt="${product.name}"></div>` : ''}
                                ${product.video ? `<div class="media-content ${!product.image ? 'active' : ''}" id="video-${product.id}">${getVideoEmbedHTML(product.video)}</div>` : ''}
                            ` : ''}
                            
                            <div class="qr-section">
                                <p><strong>Product QR Code:</strong></p>
                                <div class="qr-code">${generateProductQR(product.id, product.name)}</div>
                                <div class="url-display">${window.location.href}#product-${product.id}</div>
                            </div>
                        </div>
                    `).join('')}
                </div>
            `;
            
            container.innerHTML = html;
        }

        // Delete product
        function deleteProduct(id) {
            if (confirm('Are you sure you want to delete this product?')) {
                products = products.filter(product => product.id !== id);
                saveProducts(); // Save after deleting
                renderProducts();
                alert('✅ Product deleted successfully!');
            }
        }

        // Initialize when page loads
        loadProducts(); // Load saved products first
        generateWebsiteQR();
        renderProducts();

        // Export data function
        function exportData() {
            const dataStr = JSON.stringify(products, null, 2);
            const dataBlob = new Blob([dataStr], {type: 'application/json'});
            const url = URL.createObjectURL(dataBlob);
            const link = document.createElement('a');
            link.href = url;
            link.download = 'product-showcase-backup.json';
            link.click();
            URL.revokeObjectURL(url);
            alert('✅ Data exported successfully!');
        }

        // Import data function
        function importData() {
            document.getElementById('importFile').click();
        }

        // Handle import file
        function handleImportFile(event) {
            const file = event.target.files[0];
            if (!file) return;
            
            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const importedProducts = JSON.parse(e.target.result);
                    if (Array.isArray(importedProducts)) {
                        products = importedProducts;
                        saveProducts();
                        renderProducts();
                        alert('✅ Data imported successfully!');
                    } else {
                        alert('❌ Invalid file format');
                    }
                } catch (error) {
                    alert('❌ Error reading file');
                }
            };
            reader.readAsText(file);
            // Reset file input
            event.target.value = '';
        }

        // Allow Enter key to login
        document.getElementById('adminPassword').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                loginAdmin();
            }
        });
    </script>
</body>
</html>
